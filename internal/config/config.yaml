server:
  port: ":8080"

log:
  level: "debug"          # debug | info | warn | error
  file: "logs/gateway.log"
  max_size_mb: 50         # rotate after 50 MB
  max_backups: 7          # keep 7 old files
  max_age_days: 30        # delete files older than 30 days